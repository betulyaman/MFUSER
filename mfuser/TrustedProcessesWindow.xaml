<Window x:Class="mfuser.TrustedProcessesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Trusted Processes" Height="500" Width="720"
        Background="#1E1E1E" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0E639C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1177BB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="MutedButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#3A3A3D"/>
            <Setter Property="Padding" Value="10,5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#505054"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </Window.Resources>

    <Border Background="#252526" BorderBrush="#3F3F46" BorderThickness="1"
            CornerRadius="4" Padding="10" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" LastChildFill="False">
                <TextBlock Text="Trusted Processes"
                           Style="{StaticResource HeaderStyle}"
                           DockPanel.Dock="Left"/>
                <Button x:Name="RemoveButton" Content="Remove selected"
                        Style="{StaticResource MutedButton}"
                        DockPanel.Dock="Right"
                        ToolTip="Untrust the selected image path"
                        Click="RemoveButton_Click"/>
                <TextBlock Text="(double-click a row to remove)"
                           Foreground="#888888" FontStyle="Italic"
                           FontSize="11" VerticalAlignment="Center"
                           Margin="10,0,0,6"
                           DockPanel.Dock="Left"/>
            </DockPanel>

            <ListView Grid.Row="1" x:Name="EntriesList"
                      Background="#1E1E1E" Foreground="White"
                      BorderBrush="#3F3F46" BorderThickness="1"
                      MouseDoubleClick="EntriesList_MouseDoubleClick">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Cursor" Value="Hand"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="#" Width="40"
                            DisplayMemberBinding="{Binding Index}"/>
                        <GridViewColumn Header="Image Path" Width="500"
                            DisplayMemberBinding="{Binding ImagePath}"/>
                        <GridViewColumn Header="Status" Width="120"
                            DisplayMemberBinding="{Binding Status}"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <TextBlock Grid.Row="2" Text="Image path"
                       Style="{StaticResource LabelStyle}"
                       Margin="0,10,0,4"/>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="ImagePathTextBox"
                         KeyDown="ImagePathTextBox_KeyDown"/>
                <Button Grid.Column="1" x:Name="BrowseButton" Content="Browse..."
                        Style="{StaticResource MutedButton}" Margin="6,0,0,0"
                        Click="BrowseButton_Click"/>
                <Button Grid.Column="2" x:Name="AddButton" Content="Trust"
                        Margin="6,0,0,0" Click="AddButton_Click"/>
            </Grid>

            <DockPanel Grid.Row="4" LastChildFill="False" Margin="0,10,0,0">
                <Button x:Name="CloseButton" Content="Close"
                        Style="{StaticResource MutedButton}"
                        DockPanel.Dock="Right" Click="CloseButton_Click"/>
                <TextBlock x:Name="HintText"
                           Text="Trusted processes get the upper half of every shielded file's access-right bitmask."
                           Foreground="#888888" FontStyle="Italic" FontSize="11"
                           VerticalAlignment="Center"
                           DockPanel.Dock="Left"/>
            </DockPanel>
        </Grid>
    </Border>
</Window>
